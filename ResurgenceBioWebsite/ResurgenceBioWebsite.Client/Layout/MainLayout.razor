@inherits LayoutComponentBase

@using Microsoft.FluentUI.AspNetCore.Components

@inject NavigationManager Navigation

@* todo: implement `prefers-color-scheme` setting and theme selector. use javascript (blazor cannot access the DOM) (https://tailwindcss.com/docs/dark-mode#supporting-system-preference-and-manual-selection). can it be done in the main layout? *@
<div id="main-layout" class="min-h-screen flex flex-col items-center justify-start !bg-[#090b34]">
    <!-- Navigation Menu at the Top -->
    <header class="w-full">
        <NavMenu/>
    </header>

    <!-- Main Content Section -->
    <main class="md:prose-base container sm:prose-sm lg:prose-lg xl:prose-xl 2xl:prose-2xl mt-16">
        @Body
    </main>

    <!-- Footer or additional content could go here -->
    <div class="container">
        <Footer></Footer>
    </div>
</div>

@* <FluentToastProvider/> *@
@* <FluentDialogProvider/> *@
@* <FluentTooltipProvider/> *@
@* <FluentMessageBarProvider/> *@


@* <div id="blazor-error-ui" data-nosnippet> *@
@*     An unhandled error has occurred. *@
@*     <a href="." class="reload">Reload</a> *@
@*     <span class="dismiss">🗙</span> *@
@* </div> *@

@code
{
    private bool IsHomePage => Navigation.Uri == Navigation.BaseUri;
}